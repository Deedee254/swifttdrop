// This script has been disabled to prevent CSS conflicts
console.log('CSS copy script is disabled to prevent conflicts with Next.js CSS handling.');
console.log('The application now uses the built-in Next.js CSS import system.');